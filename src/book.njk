---
layout: item.njk
title: Book
---

<article>

  {% include "bookpromo.njk" %}

  <h3>Check out <a href="https://approachableopensource.com/">approachableopensource.com!</a></h3>

  <a target="_blank" href="https://approachableopensource.com/"><img alt="A mock of the website" src="/img/aos-macbook-pro-mockup.png"/></a>

  <br/>

  <section class="masonry">
    {% for image in collections.all | getImageFiles('img/aos-gallery') %}
      <img src="/{{ image }}" alt="Gallery image" />
    {% endfor %}
  </section>

</article>

<aside>
  <h2>All posts related to the book</h2>

  <nav>
    <ul class="collection">
      {%- for page in collections.book | reverse  -%}
        <li class="collection__item">
          <div class="collection__article">
            <a class="collection__title" href="{{ page.url | url }}">{{ page.data.title }}</a>
            <p class="collection__summary">{{ page.data.summary }}</p>
            <ul class="collection__tags">
              <li class="pull--left">
              <relative-time datetime="{{ page.data.date }}">
              {{ page.data.date }}
              </relative-time>
              </li>
            {% for tag in page.data.tags %}
              <li>
              <a href="/tags/{{tag}}">{{tag}}</a>
              </li>
            {%- endfor -%}
            </ul>
          </div>
        </li>
      {%- endfor -%}

      {% for tag in tags %}
        <li class="black tag__link">All posts tagged <a href="/tags/{{tag.name}}">{{tag.name | toHumanUrl }}</a></li>
      {% endfor %}

    </ul>
  </nav>

</aside>
